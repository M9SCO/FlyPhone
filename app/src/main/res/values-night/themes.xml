<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlyPhone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/primary</item>

        <item name="itemTextColor">@color/white</item>
    </style>

    <style name="Theme.FlyPhone.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar" />
    <style name="Theme.FlyPhone.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

</resources>